# Blackboard Stock Advisor — STEP目標

黒板方式(Blackboard Architecture)を模倣したマルチエージェント議論システム。
初回のお題は「株購入予想」。専門家(知識源)は差し替え可能なプラグインとして設計し、
インフラ(黒板構造・コントローラー)はドメイン非依存のまま汎用に保つ。

> **免責事項**: 本システムは黒板方式というマルチエージェント設計パターンを学習・検証する
> ためのプロトタイプであり、実際の投資判断の根拠として使用することは意図していない。
> 出力はあくまで「複数の視点からの議論の再現」であり、投資助言ではない。

---

## STEP 1. 黒板の構造設計

**目的**: ドメインに依存しない、汎用的な黒板データ構造を確定する。

**やること**
- 黒板のスキーマを確定する(`goal` / `facts` / `proposals` / `open_questions` / `status`)
- JSONファイルへの永続化方式を決める(パス、書き込みタイミング、排他制御)
- 黒板の「要約」を専門家に渡す際のフォーマットを決める(生ログではなく要約を渡す)

**完了基準**
- [ ] `blackboard-data/*.json` にスキーマ通りのファイルが生成される
- [ ] スキーマがどんなお題(株購入予想以外)にも流用できる形になっている
- [ ] 要約フォーマットが定義されている

**成果物**: `blackboard/schema.md`, `blackboard-data/session-*.json`

---

## STEP 2. 専門家プラグイン形式の確定

**目的**: 専門家(知識源)を設定ファイルとして差し替え可能にする。

**やること**
- 専門家1体 = 1つのYAMLファイルという形式を確定する
- 必須フィールドを決める(`name` / `role_description` / `system_prompt` / `min_confidence_to_speak`)
- 株購入予想向けの初期専門家セットを作成する(下記参照)

**完了基準**
- [ ] `experts/*.yaml` が5体分揃っている
- [ ] YAMLファイルを追加・削除するだけで専門家の増減ができる設計になっている

**成果物**: `experts/*.yaml`

---

## STEP 3. 自己採点(発火判定)ロジックの実装

**目的**: 各専門家が「自分の出番かどうか」を安価に自己判定する仕組みを作る。

**やること**
- 軽量モデル(Haiku等)に役割説明+黒板要約を渡し、0〜10の自信度スコアのみを返させる
- 閾値(例: 6点未満は発言権なし)を設定する
- 全専門家が閾値未満の場合の停止条件(quiescence)を実装する

**完了基準**
- [x] 専門家ごとに自信度スコアが取得できる
- [x] 最高スコアの専門家のみが選出される
- [x] 全員低スコア時に自動停止する

**成果物**: コントローラーの発火判定モジュール
(`src/scoring/`, `src/controller/firing.ts`, `scripts/run-firing.ts`。設計メモ: `docs/step3-firing.md`)

---

## STEP 4. 本回答生成と黒板への書き込み

**目的**: 選出された専門家が実際に貢献内容を生成し、黒板を更新する。

**やること**
- 選出された専門家のsystem_promptと黒板の最新状態を使い、本番モデル(Sonnet等)で回答生成
- 生成内容を `facts` / `proposals` / `open_questions` のどこに追加するか分類する
- 更新後の黒板をJSONファイルに保存する

**完了基準**
- [x] 専門家の発言が正しいセクションに追記される
- [x] 更新のたびにJSONファイルが上書き保存される
- [x] 更新履歴(誰がいつ何を書いたか)がログとして残る

**成果物**: コントローラーの本回答生成モジュール
(`src/generation/`, `src/blackboard/apply.ts`, `src/controller/turn.ts`, `scripts/run-turn.ts`。設計メモ: `docs/step4-generation.md`)

---

## STEP 5. Discordへの投稿

**目的**: 黒板の更新を人間が読める形でチャットに流す。

**やること**
- 黒板更新のたびに、該当チャンネルへ整形済みメッセージを投稿する
- 専門家ごとに発言の見た目を区別する(名前・色・アイコン等)
- 人間からの追加発言(新しい論点や情報)を黒板の `facts` に反映する経路を作る

**完了基準**
- [x] Discord上で誰の発言か一目でわかる(専門家ごとに絵文字 + 埋め込み色を固定)
- [x] 人間の発言が次回の自己採点判定に反映される(接頭辞なしの発言 → 黒板の facts に追加)
- [x] セッション終了時に議論の要約が投稿される(quiescence + 未解決0件で要約を生成・投稿)

**成果物**: Discord Bot連携部分(ローカルPCホスティング)
(`src/discord/`, `src/generation/summarizer.ts`, `scripts/run-bot.ts`。設計メモ: `docs/step5-discord.md`)

---

## STEP 6(拡張). 専門家による Web 調査

**目的**: 各専門家が本回答生成の前に自分で調査し、裏付け付きの意見を出せるようにする。

**やること**
- `experts/*.yaml` の任意フィールド `tools`(WebSearch / WebFetch)で調査可否を専門家ごとに制御
- 調査可の専門家は本回答生成を複数ターン許可し、Web 調査 → 裏付け付きで proposals / facts を書く
- 参照した URL を `sources` として黒板と Discord 投稿に残す
- Discord では1ターンごとに実行の可否をユーザーに確認してから進める(`!run` / ✅ リアクション)

**完了基準**
- [x] 調査ツールを持つ専門家だけが WebSearch / WebFetch を使える(`tools: []` で他ツールは遮断)
- [x] proposals / facts に `sources` が付き、ログにツール使用回数が残る
- [x] Discord で各ターン前に承認を求める(`!run` → ✅/🛑 または `!next`/`!stop`)
- [x] 実在銘柄のサンプル(`blackboard-data/session-nvda.json`)で動作確認

**成果物**: `experts/*.yaml` の `tools`、`src/generation/agent-sdk-generator.ts` の調査ループ、
Discord の承認フロー。設計メモ: `docs/step6-research.md`

---

## 初期専門家セット(株購入予想向け・5体)

| 専門家 | 専門領域 | 主な論点 |
|---|---|---|
| ファンダメンタルズ専門家 | 財務諸表・業績・企業価値 | 売上・利益・PER/PBR等の指標 |
| テクニカル専門家 | チャート・値動きパターン | トレンド・出来高・移動平均等 |
| マクロ・センチメント専門家 | 市場全体・金利・景気動向 | 金融政策・セクター全体の地合い |
| リスク専門家 | 下振れ要因・不確実性 | 財務リスク・規制リスク・分散の観点 |
| 反証専門家(Devil's Advocate) | 他の専門家の提案への反論 | 楽観的な提案に対する反対意見の提示 |

反証専門家を加えているのは、前回話した「討論は同調バイアスを起こしやすい」という懸念への対策で、
黒板方式の中に意図的に「他者を疑う役」を混ぜておく設計。

## 全体の進め方

1. STEP 1・2(黒板構造+専門家定義)を先に固める。ここまではコード不要、設計書のみで進行可能
2. STEP 3(自己採点)を最小構成で実装し、5体が正しく点数を返すか確認
3. STEP 4(本回答生成)を繋ぎ、実際に黒板が育っていく様子を確認
4. STEP 5でDiscordに繋いで人間が参加できるようにする
5. STEP 6で専門家に Web 調査をさせ、裏付け付きの議論にする
